<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Review Homepage</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100 flex flex-col h-screen">

    <nav class="bg-blue-500 p-6 text-white flex justify-between items-center">
        <a href="/"><h1 class="text-2xl font-bold">Book.Review</h1></a>
        <div class="space-x-2 flex">
            <a href="/book/add"><button class="bg-green-700 hover:bg-green-800 text-white px-4 py-2 rounded-md">&plus; Add Review</button></a>
            <form action="/sort" method="POST" class="flex space-x-2">
                <button class="bg-blue-700 hover:bg-blue-800 text-white px-4 py-2 rounded-md" type="submit" name="orderValue" value="ASC">Sort &#8593;</button>
                <button class="bg-blue-700 hover:bg-blue-800 text-white px-4 py-2 rounded-md" type="submit" name="orderValue" value="DESC">Sort &#8595;</button>
            </form>
        </div>
    </nav>
    

    <div class="container mx-auto flex-1 my-8">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            <% books.forEach(book => { %>
                <div class="bg-white p-4 rounded-lg shadow-md flex flex-col h-full">
                    <img src="<%= book.imageUrl %>" alt="<%= book.title %>" class="mb-4 object-cover h-48">
                    <h2 class="text-xl font-bold mb-2"><%= book.title %></h2>
                    <p class="text-gray-600 mb-4"><%= book.author %></p>
                    <p class="mb-4 flex-1"><%= book.review %></p>
                    <div class="flex justify-end">
                        <a href="/book/<%= book.id %>"><button class="bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-700">More</button></a>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <footer class="bg-gray-800 text-white text-center py-2">
        <p>&copy; 2023 Adlee Afif GitHub. All rights reserved.</p>
    </footer>

</body>

</html>
